# https://app.codesignal.com/challenge/QR2CL6wQadp9R6w8Y
#def dotProduct(v1, v2):
v,w = eval(dir()[0])
return sum(i * j for i, j in zip(v,w))
#return sum(i[0] * i[1] for i in zip(v,w))


#dotProduct = numpy.dot


# Solution by angelov
#dotProduct = lambda a,b: sum(i*j for i,j in zip(a,b))